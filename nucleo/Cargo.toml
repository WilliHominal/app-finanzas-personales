[package]
name = "nucleo"
version = "0.1.0"
edition = "2021"
description = "Núcleo financiero — dominio puro, sin dependencias de framework"

[dependencies]
rust_decimal = { version = "1", features = ["maths"] }
